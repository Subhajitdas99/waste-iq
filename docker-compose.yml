services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: wasteiq
      POSTGRES_USER: wasteiq
      POSTGRES_PASSWORD: wasteiq
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    depends_on:
      - db
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - "5173:80"

  agent:
    build:
      context: ./agent
    env_file:
      - ./agent/.env
    ports:
      - "8001:8001"

volumes:
  postgres_data:
